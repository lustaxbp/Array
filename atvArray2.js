/* nome:Luís Otávio Bordin
2- Exibir os números pares de 0 a 60, fazer uso do laço
de repetição for */
var numerosPares = 0;
for (var x = 0; x <= 60; x += 2) {
    numerosPares = x;
    console.log("".concat(numerosPares));
}
